define(['React', 'reactDom', 'components/Login/Login'], function (React, ReactDOM, Login) {
    'use strict';

    //var ReactTestUtils = React.addons.TestUtils;

    describe('Login', function () {

        describe('verifyUser', function () {

            it('should log in when email and password are correct', function () {
                //var email = this.refs.email;
                //email.value = 'a@a';
                //
                //var password = this.refs.email;
                //password.value = '123456';
                //
                //var instance = React.createElement(Login);
                //expect(instance.verifyUser()).toEqual(true);

            });

            it('should fail when email and password are incorrect', function () {

            });
        });

    });

});
