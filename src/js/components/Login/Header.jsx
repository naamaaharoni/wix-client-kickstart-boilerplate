define(['lodash', 'react'], function (_, React) {
    'use strict';

    return React.createClass({
        render: function () {
            return (
                <div>
                    <img src='./images/site_logo.png'/>
                    <h1>MarkBook</h1>
                </div>
            );
        }
    });
});
